QUESTION-Given an integer array nums, find a contiguous non-empty subarray within the array that has the largest product, and return the product.

The test cases are generated so that the answer will fit in a 32-bit integer.

A subarray is a contiguous subsequence of the array.

 

Example 1:

Input: nums = [2,3,-2,4]
Output: 6
Explanation: [2,3] has the largest product 6. Maximum Product Subarray


SOLUTION-//😎 © MukeshKumarBagaria

class Solution {
public:
    int maxProduct(vector<int>& nums) {
        
        //T.C->o(n^2)  and S.C->O(N) SOLUTION || 187 / 188 test cases passed.

        // int n=nums.size();
        //  int ans = INT_MIN;
        // for(int i=0;i<n;i++){
        //     int product=1;
        //     for(int j=i;j<n;j++){
        //         product=product*nums[j];
        //         ans=max(ans,product);
        //     }
        //     product=1;
        // }
        // return ans;
        
        //T.C-> O(N) SOLUTION
        
    }
};




